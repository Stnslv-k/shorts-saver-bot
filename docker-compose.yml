services:
  bot:
    build: .
    restart: unless-stopped
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml:ro
      - /Users/trblmkr/Job/ObsidianLibrary:/Users/trblmkr/Job/ObsidianLibrary
    environment:
      - PYTHONUNBUFFERED=1
    # Uncomment to co-locate with a local Ollama instance:
    # extra_hosts:
    #   - "host.docker.internal:host-gateway"
